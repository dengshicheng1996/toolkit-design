import * as React from 'react'
import Tracker from '../index'
import Button from '../../Button'

// Tracker.setTracker(amdTracker)

export default () => {
  return <Button type="primary">点击查看控制台</Button>
}
